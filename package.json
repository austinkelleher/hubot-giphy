{
  "name": "hubot-giphy",
  "description": "hubot interface for giphy-api",
  "version": "0.1.0",
  "author": "Pat Sissons <patricksissons@gmail.com>",
  "license": "MIT",
  "keywords": [
    "hubot",
    "hubot-scripts",
    "giphy"
  ],
  "repository": {
    "type": "git",
    "url": "git://github.com/patsissons/hubot-giphy.git"
  },
  "bugs": {
    "url": "https://github.com/patsissons/hubot-giphy/issues"
  },
  "dependencies": {
    "coffee-script": "^1.10.0",
    "giphy-api": "^1.1.15"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "dotenv": "^2.0.0",
    "gulp": "^3.9.1",
    "mocha": "^2.4.5",
    "sinon": "^1.17.3",
    "sinon-chai": "^2.8.0"
  },
  "main": "index.coffee",
  "scripts": {
    "test": "gulp test:npm",
    "mocha": "mocha --compilers coffee:coffee-script/register --recursive --reporter spec ./test/**/*.spec.coffee",
    "debug": "mocha --compilers coffee:coffee-script/register --recursive --reporter spec ./test/**/*.spec.coffee --debug-brk",
    "watch": "mocha --compilers coffee:coffee-script/register --recursive --reporter spec ./test/**/*.spec.coffee --watch"
  }
}
